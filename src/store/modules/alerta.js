//import axios from 'axios';

const state = {
  mensajeAlerta: '',
};

const getters = {
  mensajeAlerta: (state) => state.mensajeAlerta,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
