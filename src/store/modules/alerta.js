//import axios from 'axios';

const state = {
  mensaje: '',
};

const getters = {
  mensaje: (state) => state.mensaje,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
  namespaced: true,
};
